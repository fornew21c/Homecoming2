# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# 생성물 — Tools/generate_project.rb 로 언제든 다시 만든다.
# 절대경로가 들어가 있어 다른 사람 기계에서 그대로 열리지 않는다.
Homecoming.xcodeproj/

# 서버 데이터 — 위치 이력이 들어 있다. 절대 커밋하지 않는다.
# DB 경로가 cwd 기준이라, 시험을 `cd Server` 에서 돌리면 `Server/Server/` 가 생긴다
# (CLAUDE.md 가 시키는 그 방식이다). 내용은 같은 것이라 지워도 된다.
Server/Server/
Server/*.sqlite
Server/*.sqlite-wal
Server/*.sqlite-shm

# 인증 키
*.p8

# 비밀값 — APNs 키 경로, 공공데이터 서비스 키.
# 이 파일이 커밋되면 키가 저장소 이력에 영구히 남는다.
.env.local
Server/.env.local

.DS_Store
.route-dump
__pycache__/
*.pyc
